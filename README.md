# AmplifyingCareers Website

Next.js website scaffold for the AmplifyingCareers site.

## Local setup

Requires Node.js and npm installed.

```bash
npm install
npm run dev
```

The `public/logo.png` asset is copied from `brand_assets/logo.png` and the site uses the brand colours defined in `app/globals.css`.
